name: Update sitemap.xml

on:
  push:
    branches: [main]
    paths:
      - 'links/site/posts.json'
      - 'links/fb/posts.json'
      - 'scripts/generate_sitemap.py'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update-sitemap:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Regenerate sitemap.xml
        run: python3 scripts/generate_sitemap.py

      - name: Ping IndexNow (Yandex)
        run: python3 scripts/ping_indexnow.py

      - name: Commit changes if any
        run: |
          if git diff --quiet -- sitemap.xml scripts/.indexnow_state.json; then
            echo "Nothing to commit."
          else
            git config user.name "sitemap-bot"
            git config user.email "actions@users.noreply.github.com"
            git add sitemap.xml scripts/.indexnow_state.json
            git commit -m "Auto-update sitemap.xml and IndexNow state"
            git push
          fi
